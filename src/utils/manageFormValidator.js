export default function manageFormValidator(values) {
  // TODO validate the values
  /**
   * name required
   * url required
   * dropdowns required
   */

  return { success: true, errors: new Map() };
}
